package own.nio;

import own.nio.request.TrackCommand;
import own.nio.request.TrackDirectoryTree;

import java.io.IOException;
import java.nio.file.Path;

public class check {
    static void main() throws IOException {
        /*Path p1 = Path.of("/home/alex/a/1.txt");
        Path p2 = Path.of("/home/alex/a/2.txt");
        TrackDirectoryTree tree = new TrackDirectoryTree(p1, p2, p2);
        System.out.println(tree.compareFiles(p1, p2));;*/
    }
}

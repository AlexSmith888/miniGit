miniGit V1:
    - commands : init, commit, 
            track , history, restore, diff
    - single threaded mode(changes do not pull automatically)
    - to track changes against staging area the 
    track command is used before commit